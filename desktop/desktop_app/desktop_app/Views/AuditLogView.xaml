<UserControl x:Class="desktop_app.Views.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:desktop_app.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Style="{StaticResource UserControlBase}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <UserControl.DataContext>
        <viewmodels:AuditLogViewModel/>
    </UserControl.DataContext>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ================= HEADER ================= -->
        <Border Grid.Row="0" Style="{StaticResource HeaderWrapper}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Título -->
                <DockPanel Grid.Row="0" LastChildFill="False">
                    <TextBlock DockPanel.Dock="Left"
                               Text="Historial de Auditoría"
                               Style="{StaticResource HeaderMainTitle}"/>
                </DockPanel>

                <!-- Filtros -->
                <Border Grid.Row="1"
                        Background="#F7FAFD"
                        CornerRadius="14"
                        Padding="12"
                        Margin="0,14,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="1.4*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,10">
                            <Label Content="Acción" Style="{StaticResource LabelForm}" Margin="0,0,0,4"/>
                            <ComboBox Style="{StaticResource DropdownStyle}"
                                      ItemsSource="{Binding FilterOptions}"
                                      SelectedItem="{Binding SelectedFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,10,10">
                            <Label Content="Colección" Style="{StaticResource LabelForm}" Margin="0,0,0,4"/>
                            <ComboBox Style="{StaticResource DropdownStyle}"
                                      ItemsSource="{Binding FilterCollectionOptions}"
                                      SelectedItem="{Binding SelectedCollectionFilter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,10">
                            <Label Content="Rango de fechas" Style="{StaticResource LabelForm}" Margin="0,0,0,4"/>
                            <StackPanel Orientation="Horizontal">
                                <DatePicker Width="160"
                                            Language="es-ES"
                                            SelectedDate="{Binding FilterFromDate, Mode=TwoWay}"
                                            Style="{StaticResource DatePickerForm}"/>
                                <TextBlock Text=" - " VerticalAlignment="Center" Margin="4,0"/>
                                <DatePicker Width="160"
                                            Language="es-ES"
                                            SelectedDate="{Binding FilterToDate, Mode=TwoWay}"
                                            Style="{StaticResource DatePickerForm}"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Grid.Row="1"
                                    Grid.ColumnSpan="3"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Right">
                            <Button Style="{StaticResource ClearFiltersBtn}"
                                    Content="Limpiar"
                                    Command="{Binding ClearFiltersCommand}"
                                    Height="38"
                                    Padding="15,0"
                                    Margin="0,0,10,0"
                                    FontSize="14"/>
                            <Button Style="{StaticResource SaveBtn}"
                                    Content="Buscar"
                                    Command="{Binding SearchCommand}"
                                    Height="38"
                                    Width="120"
                                    FontSize="14"
                                    FontWeight="SemiBold"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <!-- ===== Status + Refrescar ===== -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding StatusText}"
                       Foreground="#6B7280"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Style="{StaticResource SaveBtn}"
                    Content="Refrescar"
                    Command="{Binding RefreshCommand}"/>
        </Grid>

        <!-- ===== LOADING ===== -->
        <TextBlock Grid.Row="2" Text="Cargando historial..."
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                   HorizontalAlignment="Center" VerticalAlignment="Top"
                   Margin="0,10,0,0" Foreground="#6B7280" FontSize="14"/>

        <!-- ===== TABLA (scroll solo aquí; altura máxima para no expandir la ventana) ===== -->
        <ListView Grid.Row="2" 
                  x:Name="AuditLogsListView"
                  ItemsSource="{Binding Logs}" 
                  SizeChanged="ListView_SizeChanged"
                  MouseDoubleClick="AuditLogsListView_MouseDoubleClick"
                  AlternationCount="2" 
                  ItemContainerStyle="{StaticResource AlternatingListViewItemStyle}" 
                  Margin="0,5,0,0" 
                  BorderThickness="0.5"
                  MaxHeight="480">
            <ListView.View>
                <GridView>
                    <!-- Fecha -->
                    <GridViewColumn Header="Fecha" HeaderContainerStyle="{StaticResource TableHeader}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Timestamp, StringFormat='dd/MM/yyyy HH:mm'}" 
                                           Style="{StaticResource GridViewCellText}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Acción -->
                    <GridViewColumn Header="Acción" HeaderContainerStyle="{StaticResource TableHeader}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource AuditActionStyle}" HorizontalAlignment="Center" Padding="8,3">
                                    <TextBlock Text="{Binding Action}" Style="{StaticResource GridViewCellText}" Foreground="#ffffff" Padding="0"/>
                                </Border>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Tipo de entidad -->
                    <GridViewColumn Header="Entidad" HeaderContainerStyle="{StaticResource TableHeader}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding EntityType}" 
                                           Style="{StaticResource GridViewCellText}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- ID Entidad -->
                    <GridViewColumn Header="ID" HeaderContainerStyle="{StaticResource TableHeader}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding EntityId}" 
                                           Style="{StaticResource GridViewCellText}"
                                           ToolTip="{Binding EntityId}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Resumen -->
                    <GridViewColumn Header="Resumen" HeaderContainerStyle="{StaticResource TableHeader}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding SummaryText}" 
                                           Style="{StaticResource GridViewCellText}"
                                           ToolTip="{Binding TooltipText}"
                                           ToolTipService.InitialShowDelay="100"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Actor -->
                    <GridViewColumn Header="Actor" HeaderContainerStyle="{StaticResource TableHeader}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ActorType}" 
                                           Style="{StaticResource GridViewCellText}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Detalle -->
                    <GridViewColumn Header="" HeaderContainerStyle="{StaticResource TableHeader}" Width="65">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource UpdateBookingBtn}"
                                        Content="visibility"
                                        ToolTip="Ver detalle (anterior vs nuevo)"
                                        Command="{Binding DataContext.ViewAuditDetailCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
