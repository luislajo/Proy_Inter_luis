<Window x:Class="desktop_app.Views.ChangeRoomStatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Cambiar estado habitación"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        Background="#F8F6F0">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding TitleText}"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Estado" Style="{StaticResource LabelForm}" Margin="0,0,0,4"/>
            <ComboBox Style="{StaticResource DropdownStyle}"
                      ItemsSource="{Binding StatusOptions}"
                      DisplayMemberPath="Label"
                      SelectedValuePath="Value"
                      SelectedValue="{Binding SelectedStatus, Mode=TwoWay}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <Label Content="Motivo (obligatorio si no disponible)" Style="{StaticResource LabelForm}" Margin="0,0,0,4"/>
            <TextBox Style="{StaticResource TextBoxForm}"
                     Height="70"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     Text="{Binding Reason, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <Label Content="Duración estimada (min)" Style="{StaticResource LabelForm}" Margin="0,0,0,4"/>
                <TextBox Style="{StaticResource TextBoxForm}"
                         Width="160"
                         Text="{Binding EstimatedMinutesText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Opcional"/>
            </StackPanel>

            <Button Grid.Column="1"
                    Style="{StaticResource ClearFiltersBtn}"
                    Content="Cancelar"
                    Width="120"
                    Height="38"
                    Margin="0,22,10,0"
                    Command="{Binding CancelCommand}"/>

            <Button Grid.Column="2"
                    Style="{StaticResource SaveBtn}"
                    Content="Guardar"
                    Width="120"
                    Height="38"
                    Margin="0,22,0,0"
                    Command="{Binding SaveCommand}"/>
        </Grid>
    </Grid>
</Window>

