<UserControl x:Class="desktop_app.Views.InvoicePrepareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="880"
             Style="{StaticResource UserControlBase}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource HeaderWrapper}">
            <StackPanel>
                <TextBlock Text="Factura — configuración" Style="{StaticResource HeaderMainTitle}"/>
                <TextBlock Text="{Binding RoomSummary}" Foreground="#6B7280" Margin="0,6,0,0"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,14,0,0">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="Volver" Style="{StaticResource RedReturnBtn}" Command="{Binding BackCommand}" Width="96"/>
                    <Button Content="Guardar y abrir PDF" Style="{StaticResource SaveBtn}"
                            Command="{Binding ApplyAndDownloadCommand}" Margin="10,0,0,0" MinWidth="160"/>
                    <Button Content="Descargar PDF" Style="{StaticResource SaveBtn}"
                            Command="{Binding DownloadOnlyCommand}" Margin="10,0,0,0" MinWidth="140"
                            IsEnabled="{Binding HasExistingInvoice}"/>
                </StackPanel>

                <TextBlock Text="Datos de empresa facturada" FontWeight="SemiBold" Margin="0,8,0,8"/>
                <Label Content="Nombre o razón social" Style="{StaticResource LabelForm}"/>
                <TextBox Text="{Binding CompanyName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxForm}"/>
                <Label Content="CIF" Style="{StaticResource LabelForm}"/>
                <TextBox Text="{Binding CompanyTaxId, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource TextBoxForm}"
                         ToolTip="Ejemplo de CIF de empresa en España: B12345678"/>
                <TextBlock Text="Ej.: B12345678 (empresa ficticia)"
                           Foreground="#6B7280" FontSize="11" Margin="0,-2,0,6"/>
                <Label Content="Dirección fiscal" Style="{StaticResource LabelForm}"/>
                <TextBox Text="{Binding CompanyAddress, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TextBoxForm}" TextWrapping="Wrap" MinHeight="44"/>

                <TextBlock Text="Extras de la habitación" FontWeight="SemiBold" Margin="0,12,0,4"/>
                <TextBlock Text="Si la habitación no tiene extras, la tabla puede quedar vacía."
                           Foreground="#6B7280" FontSize="12" Margin="0,0,0,6">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ShowExtrasEmptyHint}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <Grid Margin="6,2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <ListView x:Name="ExtrasListView"
                              Grid.Column="0"
                              ItemsSource="{Binding ExtraRows}"
                              AlternationCount="2"
                              ItemContainerStyle="{StaticResource AlternatingListViewItemStyle}"
                              BorderThickness="0.5"
                              MinHeight="120"
                              MaxHeight="280"
                              ScrollViewer.VerticalScrollBarVisibility="Auto"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              Focusable="False"
                              SizeChanged="ExtrasList_SizeChanged">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Concepto" Width="260" HeaderContainerStyle="{StaticResource TableHeader}">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Name}" Padding="6,3" VerticalAlignment="Center" MinHeight="22" Margin="2"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Cant." Width="72" HeaderContainerStyle="{StaticResource TableHeader}">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Quantity}" Padding="6,3" VerticalAlignment="Center" MinHeight="22" Margin="2"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="€ / u." Width="138" HeaderContainerStyle="{StaticResource TableHeader}">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding UnitPrice, StringFormat={}{0:N2}}" Padding="6,3" VerticalAlignment="Center" MinHeight="22" Margin="2"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
